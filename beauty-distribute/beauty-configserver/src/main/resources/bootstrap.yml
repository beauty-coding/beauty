server:
  port: 8060



spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/m-day-day-up/common.git
          search-paths: config-repository/mall
          username: m-day-day-up  #账号密码写真实的快一些我觉得，不使用也能访问有点慢
          password: 20181214_GIT,coki
      label: master
    bus:
      enabled: true
      trace:
        enabled: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: SpringCloud-bus
eureka:
  client:
    service-url:
      defaultZone: http://eureka0:8010/eureka/, http://eureka1:8011/eureka/, http://eureka2:8012/eureka/ # eureka server的地址

management:
  endpoints:
    web:
      exposure:
        include: "refresh"